<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================= -->
    <!-- PHOTOS INITIALES                              -->
    <!-- ============================================= -->
    
    <record id="apartment_initial_photo_wizard_view_form" model="ir.ui.view">
        <field name="name">apartment.initial.photo.wizard.form</field>
        <field name="model">apartment.initial.photo.wizard</field>
        <field name="arch" type="xml">
            <form string="Import de photos initiales">
                <group>
                    <group>
                        <field name="property_id" options="{'no_create': True}"/>
                        <field name="taken_date"/>
                    </group>
                    <group>
                        <field name="room_type_id"/>
                        <field name="photo_type"/>
                    </group>
                </group>
                <separator string="Photos à importer"/>
                <field name="photo_ids" widget="many2many_binary" string="Sélectionner les photos"/>
                <footer>
                    <button name="action_import_photos" string="Importer les photos" type="object" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="apartment_initial_photo_wizard_action" model="ir.actions.act_window">
        <field name="name">Importer des photos initiales</field>
        <field name="res_model">apartment.initial.photo.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_property_id': active_id}</field>
    </record>

    <!-- ============================================= -->
    <!-- PHOTOS DU BIEN                                -->
    <!-- ============================================= -->
    
    <record id="apartment_property_image_wizard_view_form" model="ir.ui.view">
        <field name="name">apartment.property.image.wizard.form</field>
        <field name="model">apartment.property.image.wizard</field>
        <field name="arch" type="xml">
            <form string="Import de photos du bien">
                <group>
                    <field name="property_id" options="{'no_create': True}"/>
                </group>
                <separator string="Photos à importer"/>
                <field name="photo_ids" widget="many2many_binary" string="Sélectionner les photos"/>
                <footer>
                    <button name="action_import_photos" string="Importer les photos" type="object" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="apartment_property_image_wizard_action" model="ir.actions.act_window">
        <field name="name">Importer des photos</field>
        <field name="res_model">apartment.property.image.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_property_id': active_id}</field>
    </record>

    <!-- ============================================= -->
    <!-- PHOTOS ÉTAT DES LIEUX                         -->
    <!-- ============================================= -->
    
    <record id="apartment_inventory_photo_wizard_view_form" model="ir.ui.view">
        <field name="name">apartment.inventory.photo.wizard.form</field>
        <field name="model">apartment.inventory.photo.wizard</field>
        <field name="arch" type="xml">
            <form string="Import de photos d'état des lieux">
                <group>
                    <group>
                        <field name="inventory_id" options="{'no_create': True}"/>
                    </group>
                    <group>
                        <field name="room_type_id"/>
                        <field name="photo_type"/>
                    </group>
                </group>
                <separator string="Photos à importer"/>
                <field name="photo_ids" widget="many2many_binary" string="Sélectionner les photos"/>
                <footer>
                    <button name="action_import_photos" string="Importer les photos" type="object" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="apartment_inventory_photo_wizard_action" model="ir.actions.act_window">
        <field name="name">Importer des photos</field>
        <field name="res_model">apartment.inventory.photo.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_inventory_id': active_id}</field>
    </record>

    <!-- ============================================= -->
    <!-- PHOTOS VISITE DE CONTRÔLE                     -->
    <!-- ============================================= -->
    
    <record id="apartment_visit_photo_wizard_view_form" model="ir.ui.view">
        <field name="name">apartment.control.visit.photo.wizard.form</field>
        <field name="model">apartment.control.visit.photo.wizard</field>
        <field name="arch" type="xml">
            <form string="Import de photos de visite">
                <group>
                    <group>
                        <field name="visit_id" options="{'no_create': True}"/>
                    </group>
                    <group>
                        <field name="room_type_id"/>
                        <field name="photo_type"/>
                    </group>
                </group>
                <separator string="Photos à importer"/>
                <field name="photo_ids" widget="many2many_binary" string="Sélectionner les photos"/>
                <footer>
                    <button name="action_import_photos" string="Importer les photos" type="object" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="apartment_visit_photo_wizard_action" model="ir.actions.act_window">
        <field name="name">Importer des photos</field>
        <field name="res_model">apartment.control.visit.photo.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_visit_id': active_id}</field>
    </record>

</odoo>
