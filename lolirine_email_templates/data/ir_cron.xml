<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- ============================================= -->
        <!-- CRON - ENVOI AUTOMATIQUE DES FACTURES -->
        <!-- ============================================= -->
        <record id="ir_cron_send_scheduled_invoices" model="ir.cron">
            <field name="name">Lolirine - Envoi emails factures planifiées</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="state">code</field>
            <field name="code">model._cron_send_scheduled_invoices()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
            <!-- Exécution à 7h00 chaque jour -->
            <field name="nextcall" eval="(DateTime.now().replace(hour=7, minute=0, second=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
        
    </data>
</odoo>
