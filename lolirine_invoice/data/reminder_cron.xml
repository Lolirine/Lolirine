<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- ============================================= -->
        <!-- CRON AUTO-RELANCE QUOTIDIEN                   -->
        <!-- Exécution chaque jour à 8h00                  -->
        <!-- ============================================= -->
        <record id="ir_cron_auto_reminder" model="ir.cron">
            <field name="name">Lolirine - Auto-relance factures impayées</field>
            <field name="model_id" ref="model_lolirine_invoice_reminder"/>
            <field name="state">code</field>
            <field name="code">model._cron_auto_reminder()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=6, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
        
        <!-- ============================================= -->
        <!-- CRON VÉRIFICATION FACTURES PAYÉES             -->
        <!-- Exécution chaque jour à 9h00                  -->
        <!-- ============================================= -->
        <record id="ir_cron_check_paid_invoices" model="ir.cron">
            <field name="name">Lolirine - Vérifier factures payées (relances)</field>
            <field name="model_id" ref="model_lolirine_invoice_reminder"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_paid_invoices()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=7, minute=0, second=0)).strftime('%Y-%m-%d %H:%M:%S')"/>
        </record>
        
    </data>
</odoo>
