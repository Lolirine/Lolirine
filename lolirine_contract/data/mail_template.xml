<?xml version="1.0"?>
<odoo>

    <record id="email_template_contract" model="mail.template">
        <field name="name">Contrat de Garde-Meubles</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="email_from">{{ (object.company_id.email or user.email_formatted) }}</field>
        <field name="email_to">{{ object.partner_id.email }}</field>
        <field name="subject">Votre contrat - {{ object.name }}</field>
        <field name="body_html" type="html">
            <p>Bonjour,</p>
            <p>Veuillez trouver ci-joint votre contrat de garde-meubles.</p>
            <p>Cordialement,<br/>Lolirine</p>
        </field>
        <field name="report_template_ids" eval="[(4, ref('lolirine_contract.action_report_contract'))]"/>
        <field name="auto_delete" eval="False"/>
    </record>

</odoo>
