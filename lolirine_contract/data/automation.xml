<?xml version="1.0" encoding="utf-8"?>
<odoo>
<record id="automation_send_contract_on_confirm" model="base.automation" forcecreate="True">
    <field name="name">Envoyer le contrat de garde-meubles</field>
    <field name="model_id" ref="sale.model_sale_order"/>
    <field name="trigger">on_write</field>
    <field name="filter_pre_domain">[('state', '!=', 'sale'), ('is_subscription', '=', True)]</field>
    <field name="filter_domain">[('state', '=', 'sale'), ('is_subscription', '=', True)]</field>
    <field name="state">mail_post</field>
    <field name="template_id" ref="lolirine_contract.email_template_contract"/>
    <field name="active" eval="True"/>
</record>
<record id="action_server_send_contract" model="ir.actions.server">
    <field name="name">Envoyer le contrat par email</field>
    <field name="model_id" ref="sale.model_sale_order"/>
    <field name="binding_model_id" ref="sale.model_sale_order"/>
    <field name="binding_view_types">form</field>
    <field name="state">code</field>
    <field name="code">
for record in records:
    template = env.ref('lolirine_contract.email_template_contract', raise_if_not_found=False)
    if template:
        template.send_mail(record.id, force_send=True)
    </field>
</record>
</odoo>
