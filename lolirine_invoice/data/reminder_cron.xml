<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Cron auto-relance quotidien -->
    <record id="ir_cron_auto_reminder" model="ir.cron">
        <field name="name">Relances: Generation automatique</field>
        <field name="model_id" ref="model_lolirine_invoice_reminder"/>
        <field name="state">code</field>
        <field name="code">model._cron_auto_reminder()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=6, minute=0, second=0).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="active">True</field>
    </record>
    
    <!-- Cron verification paiements -->
    <record id="ir_cron_check_paid" model="ir.cron">
        <field name="name">Relances: Verification paiements</field>
        <field name="model_id" ref="model_lolirine_invoice_reminder"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_paid()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=7, minute=0, second=0).strftime('%Y-%m-%d %H:%M:%S')"/>
        <field name="active">True</field>
    </record>

</odoo>
