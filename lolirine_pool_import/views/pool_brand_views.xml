<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- LIST VIEW -->
    <record id="pool_brand_list" model="ir.ui.view">
        <field name="name">pool.brand.list</field>
        <field name="model">pool.brand</field>
        <field name="arch" type="xml">
            <list>
                <field name="sequence" widget="handle"/>
                <field name="logo" widget="image" options="{'size': [50, 50]}"/>
                <field name="name"/>
                <field name="code"/>
                <field name="product_count"/>
                <field name="active" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- FORM VIEW -->
    <record id="pool_brand_form" model="ir.ui.view">
        <field name="name">pool.brand.form</field>
        <field name="model">pool.brand</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(lolirine_pool_import.action_pool_supplier)d"
                                type="action"
                                class="oe_stat_button"
                                icon="fa-cube"
                                invisible="product_count == 0">
                            <field name="product_count" widget="statinfo" string="Produits"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archivé" bg_color="text-bg-danger"
                            invisible="active"/>
                    <field name="logo" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1><field name="name" placeholder="Nom de la marque"/></h1>
                    </div>
                    <group>
                        <group string="Informations">
                            <field name="code"/>
                            <field name="sequence"/>
                            <field name="website_url" widget="url"/>
                            <field name="main_supplier_id"/>
                            <field name="active" invisible="1"/>
                        </group>
                    </group>
                    <group string="Description">
                        <field name="description" nolabel="1" colspan="2"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ACTION -->
    <record id="action_pool_brand" model="ir.actions.act_window">
        <field name="name">Marques</field>
        <field name="res_model">pool.brand</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
